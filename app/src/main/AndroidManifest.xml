<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:name=".OriginIsleApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OriginIsle">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Posts / updates / ends OriginIsland SuperX cards. Special-use FGS so it can
             keep re-posting a live card (e.g. a ticking football score). -->
        <!-- stopWithTask=false + START_STICKY get casting back after a swipe-away: OriginOS kills
             the process, so recovery comes from the system restarting this service, which rebinds
             the listener with it. Gated on vivo's "Associated startup" — with that off nothing
             recovers until a reboot, which is why onboarding pushes it. -->
        <service
            android:name=".island.PlaygroundService"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="OriginIsland live update service" />
        </service>

        <!-- Listens to every notification on the device and re-casts eligible into the OriginIsland. -->
        <service
            android:name=".service.NotificationCastListener"
            android:exported="true"
            android:label="Пространство Origin (Слушатель уведомлений)"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Routes media-button taps from a cast media card back to the source app. -->
        <receiver
            android:name=".service.MediaControlReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.lenovoved.android.MEDIA_PLAY_PAUSE" />
                <action android:name="com.lenovoved.android.MEDIA_NEXT" />
                <action android:name="com.lenovoved.android.MEDIA_PREV" />
                <action android:name="com.lenovoved.android.MEDIA_SEEK" />
                <action android:name="com.lenovoved.android.MEDIA_CUSTOM" />
            </intent-filter>
        </receiver>

        <!-- Wavelet global audio session receiver for media players -->
        <receiver
            android:name=".wavelet.AudioSessionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.action.OPEN_AUDIO_EFFECT_CONTROL_SESSION" />
                <action android:name="android.media.action.CLOSE_AUDIO_EFFECT_CONTROL_SESSION" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
